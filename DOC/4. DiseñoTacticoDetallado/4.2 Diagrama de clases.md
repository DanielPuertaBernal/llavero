# 4.2 Diagrama de clases

Este diagrama es distinto al de `2.1 Modelo Dominio Anémico.md`: aquel mostraba **datos y relaciones**; este muestra **clases de código y sus operaciones**, siguiendo la convención de 5 capas por módulo ya decidida en `estrategia-migracion/backend.md`. Todos los módulos de Llavero repiten el mismo patrón — aquí se muestra primero el patrón genérico, y luego un ejemplo real y completo con el módulo `llaves`.

## Patrón genérico (se repite en los 15 módulos)

```mermaid
classDiagram
    class XController {
        <<controller.py — router Ninja>>
        +get(request) HttpResponse
        +post(request, payload) HttpResponse
    }
    class XService {
        <<service.py>>
        +ejecutar_caso_de_uso(datos) Resultado
    }
    class XDomain {
        <<domain.py — funciones puras>>
        +calcular_algo(datos) Resultado
        +validar_algo(datos) bool
    }
    class XRepository {
        <<repository.py — única puerta al ORM>>
        +obtener_por_id(id) XModel
        +crear(datos) XModel
        +actualizar(id, datos) XModel
    }
    class XModel {
        <<model.py — ORM, solo datos>>
    }

    XController --> XService : llama
    XService --> XDomain : usa
    XService --> XRepository : usa
    XRepository --> XModel : persiste
```

**Regla dura** (ya establecida): `XController` nunca llama a `XDomain` ni a `XRepository` directamente — todo pasa por `XService`, que es la única API pública del módulo hacia otros módulos también.

## Ejemplo real: módulo `llaves`

Basado en los métodos que ya existen y funcionan en el sistema actual (`llave.domain.js`, `llave.service.js`, `programacion.service.js`), traducidos a la convención nueva e incorporando las decisiones ya tomadas (`origen`, `docente_titular_id`/`reclamado_por_id`, `tipo_entrega`, ubicación como fotografía).

```mermaid
classDiagram
    class LlaveController {
        <<controller.py>>
        +get_pendientes(request) HttpResponse
        +get_historial(request, filtros) HttpResponse
        +post_procesar_credencial(request, id_carnet) HttpResponse
        +post_entregar_manual(request, payload) HttpResponse
        +patch_devolver(request, llave_id, payload) HttpResponse
    }

    class LlaveService {
        <<service.py>>
        +procesar_lectura_credencial(id_carnet, usuario) Resultado
        +registrar_entrega_manual(datos, usuario) Llave
        +registrar_devolucion(llave_id, usuario, novedad) Llave
        +confirmar_prestamo_anticipado(datos, usuario) Llave
        +obtener_historial(filtros, paginacion) list
    }

    class LlaveDomain {
        <<domain.py — funciones puras>>
        +encontrar_clase_actual(clases, hora) Clase
        +match_monitor_clase(delegacion, clase) bool
        +horario_cubierto_por_prestamo(horario, procesados) bool
        +resolver_origen(clase_o_reserva) str
        +calcular_estado_mora(fecha_entrega, limite_min) str
        +construir_datos_entrega(persona, clase, usuario) dict
        +construir_datos_devolucion(llave, usuario, novedad) dict
    }

    class LlaveRepository {
        <<repository.py>>
        +obtener_pendientes() list
        +obtener_activa_por_docente_y_aula(docente_id, aula_id) Llave
        +obtener_historial(filtros, paginacion) list
        +crear_prestamo(datos) Llave
        +marcar_devuelta(llave_id, datos) Llave
        +actualizar_estado(llave_id, estado) Llave
    }

    class LlaveModel {
        <<model.py>>
    }

    LlaveController --> LlaveService : llama
    LlaveService --> LlaveDomain : usa
    LlaveService --> LlaveRepository : usa
    LlaveRepository --> LlaveModel : persiste

    LlaveService ..> ProgramacionService : consulta clase del día
    LlaveService ..> ComunidadService : identifica persona por credencial
    LlaveService ..> MonitorService : valida delegación
    LlaveService ..> ConfiguracionService : lee límite antes de mora
```

Las flechas punteadas (`..>`) al final son las únicas conexiones permitidas hacia otros módulos — siempre contra el `Service` del módulo ajeno, nunca contra su `Repository` ni su `Model` (regla dura de `backend.md`).

### Qué hace cada capa aquí, en concreto

- **`LlaveDomain`**: es donde vive la lógica que hoy resuelve, sin tocar la base de datos, si un tap de credencial es una entrega o una devolución, si un monitor delegado coincide con la clase que se está reclamando, y si el préstamo ya pasó el límite configurado de mora. Al ser funciones puras, se prueban con datos de entrada/salida simples, sin mockear Postgres — es la pieza que primero se cubre con TDD.
- **`LlaveService`**: orquesta — llama a `ProgramacionService`/`ComunidadService`/`MonitorService`/`ConfiguracionService` (nunca a sus modelos directamente) para reunir los datos que `LlaveDomain` necesita, y envuelve la escritura final en `transaction.atomic()` para que crear el préstamo sea una operación indivisible.
- **`LlaveRepository`**: los únicos métodos que tocan el ORM de `LlaveModel`. Nótese que no hay un `find()` genérico — cada método dice qué resuelve (`obtener_activa_por_docente_y_aula`, no `findOne(filtro)`).

---

[Volver](../README.md)
