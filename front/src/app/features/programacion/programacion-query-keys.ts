/**
 * Claves de TanStack Query de la feature `programacion`, centralizadas acá
 * igual que en `catalogos`/`reservas`/`llaves`.
 */
export const programacionQueryKeys = {
  semestres: ['programacion', 'semestres'] as const,
  lista: ['programacion', 'lista'] as const,
};
